```{R}
getwd()
setwd("/home/srdevl058/Desktop/R-tutorial")

read.csv("input.csv") #output is a DataFrame
write.csv(BMI,"output.csv")
```


```{R}
read.xlxs("input.xlxs", sheetIndex = 1)

```


```{R}
# Sometimes, the data generated by other programs are required to be processed by R as a binary file. Also R is required to create binary files which can be shared with other programs.

filename <- file("/web/com/binmtcars.dat", "rb")
bindata <- readBin(filename, integer(),  n = 18) #integer is for what is file name, n is no. of cloumns

filename = file("/web/com/binmtcars.dat", "wb")
writeBin(bindata, filename)
```


```{R}
library("rjson")
json_file = fromJSON(file = "input.json")
as.as.data.frame(json_file)
```


```{R}
# note you can store json like data in xml too.

library("XML")
library("methods")

xmlParse(file = "input.xml")
xmlToDataFrame("input.xml")
```


```{R}
library("RMySQL")

mysqlconnection = dbConnect(MySQL(), user = 'root', password = '', dbname = 'sakila',host = 'localhost')

dbListTables(mysqlconnection)
result = dbSendQuery(mysqlconnection, "select * from actor")
fetch(result, n = 5)   #the results are dataframe
```

```{R}
# install.packages("RCurl")
# install.packages("XML")
# install.packages("stringr")
# install.packages("plyr")
url <- "http://www.geos.ed.ac.uk/~weather/jcmb_ws/"
links <- getHTMLLinks(url)
links_list <- as.list(links)
download.file(links_list[1],'abc')
```
